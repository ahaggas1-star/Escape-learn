# packages/database — أنواع وعميل قاعدة البيانات

أنواع TypeScript المولّدة من مخطط Supabase، وعميل قاعدة البيانات المشترك.

> **الحالة:** Placeholder. المخطط في `supabase/migrations/`.

## خطوات لاحقة
- توليد الأنواع: `supabase gen types typescript` بعد تطبيق المايقريشن.
- تصدير عميل Supabase مهيأ (URL + Anon Key من المتغيرات).
