# سجل المهام المعلّقة — BACKLOG

مهام مؤجّلة لما بعد الـMVP، مرتّبة حسب الأولوية. القرارات الجوهرية محسومة في
`OWNER_DECISIONS.md`؛ هذه مهام **تنفيذية** تتطلب عملًا إضافيًا.

## أولوية عالية

- [ ] **رفع الصور كإثبات** (قرار #5 معتمد): تهيئة Supabase Storage bucket + سياسات
      وصول + واجهة رفع في تنفيذ المهمة، وتسجيل الموافقة في `consent_logs`.
- [ ] **ترقية المستخدم لطاقم المحتوى**: واجهة لمدير النظام لتعيين الأدوار
      (`content_manager`/`system_admin`) بدل تحديثها يدويًا عبر SQL.
- [ ] **تعطيل تأكيد البريد** في إعدادات Supabase Auth لتجربة أسرع (أو تخصيص
      رسالة التأكيد العربية).

## أولوية متوسطة

- [ ] **المكافآت المادية** (قرار #6): إدارة كتالوج مكافآت مادية يحدّدها ولي الأمر،
      وربطها بصناديق المكافآت.
- [ ] **إكمال الهدف تلقائيًا**: تحديث `goals.status='completed'` عند اعتماد كل مهامه.
- [ ] **تعديل/حذف القوالب** في لوحة الإدارة (الحالي: إضافة + نشر فقط).
- [ ] **إدارة الشارات/المستويات/الإنجازات** من لوحة الإدارة (قرار #15).
- [ ] **مهام لوحة الابن للمراحل العمرية**: تصفية القوالب المعروضة حسب مرحلة الابن.
- [ ] **التحديات العائلية** (`family_challenges`): إنشاؤها ومتابعتها في الواجهة.

## أولوية منخفضة / مستقبلي

- [ ] فصل `apps/admin` كتطبيق مستقل (حاليًا مدمج تحت `/admin` في `apps/web`).
- [ ] دعم i18n كامل (قرار #13: العربية أساسًا الآن).
- [ ] PWA: manifest + service worker (قرار #12: Web/PWA).
- [ ] هوية بصرية رسمية معتمدة (قرار #14: هوية فرائحية مبدئية حاليًا).
- [ ] لوحة مدير النظام: سجلات النظام، الموافقات، حذف بيانات الأسرة/الابن.
- [ ] اختبارات آلية (unit/e2e) وCI.
- [ ] توليد أنواع TypeScript من مخطط Supabase (`supabase gen types`).

## ملاحظات تقنية

- الاسم الداخلي `ghars` (سكيمة/رموز) ثابت؛ الاسم الظاهر «قِيَم».
- بيئة العرض الحالية: سكيمة `ghars` داخل مشروع Supabase مشترك (انظر `supabase/README.md`).
