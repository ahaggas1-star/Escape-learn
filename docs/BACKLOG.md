# سجل المهام المعلّقة — BACKLOG

مهام مؤجّلة لما بعد الـMVP، مرتّبة حسب الأولوية. القرارات الجوهرية محسومة في
`OWNER_DECISIONS.md`؛ هذه مهام **تنفيذية** تتطلب عملًا إضافيًا.

## أولوية عالية

- [x] **رفع الصور كإثبات** (قرار #5): صندوق Storage خاص `qiyam-proofs` + سياسات +
      واجهة رفع في تنفيذ المهمة + عرض الصورة في المراجعة عبر رابط موقّع + تسجيل الموافقة.
- [x] **ترقية المستخدم لطاقم المحتوى**: واجهة مدير النظام `/admin/users` لتعيين الأدوار.
- [x] **التسجيل بدون تأكيد بريد**: مشغّل تأكيد تلقائي (`0009_auto_confirm`) + دخول
      مباشر بعد التسجيل. (للإنتاج: يُفضّل تأكيد بريد حقيقي.)

## مطلوب من صاحب المشروع (بعد المرحلة 2)

- [x] **صناديق مكافآت ينشئها ولي الأمر** ويضع فيها رسائله (`/rewards`).
- [x] **نظام عملات (Coins)**: يكسبها الطفل تلقائيًا من نقاط الخبرة ويصرفها على فتح الصناديق.
- [x] **3 أنواع صناديق** (عادية/مميزة/رهيبة) بتصاميم وتكلفة يحدّدها ولي الأمر (أو مجانية).

## أولوية متوسطة

- [x] **المكافآت المادية** (قرار #6): صفحة `/rewards` لولي الأمر لإدارة عناصر صندوق
      الأسرة (مادية/امتياز/تقدير/رسالة).
- [x] **إكمال الهدف تلقائيًا**: تحديث `goals.status='completed'` عند اعتماد كل مهامه.
- [x] **حذف القوالب** في لوحة الإدارة. (يبقى: **تعديل** القوالب لاحقًا)
- [x] **إدارة الشارات/المستويات/الإنجازات** من لوحة الإدارة (`/admin/gamification`).
- [x] **تعديل القوالب**: صفحات تعديل للأهداف والمهام في لوحة الإدارة.
- [x] **تصفية قوالب المهام حسب مرحلة الابن** في صفحة الهدف.
- [x] **التحديات العائلية** (`family_challenges`): إنشاؤها ومنح إنجازها في `/challenges`.

## أولوية منخفضة / مستقبلي

- [ ] فصل `apps/admin` كتطبيق مستقل (حاليًا مدمج تحت `/admin` في `apps/web`).
- [ ] دعم i18n كامل (قرار #13: العربية أساسًا الآن).
- [x] PWA: manifest + أيقونة + Service Worker (عمل جزئي دون اتصال + صفحة offline) — قرار #12.
- [ ] هوية بصرية رسمية معتمدة (قرار #14: هوية فرائحية مبدئية حاليًا).
- [x] شعار (SVG) + صفحة هبوط تعريفية (الصفحة الرئيسية للزوار).
- [x] لوحة مدير النظام (`/admin/system`): سجلات الموافقات والعمليات + حق الحذف
      لولي الأمر (`/settings`) مع تسجيلها في audit عبر `log_audit`.
- [x] اختبارات آلية (Vitest لمنطق التلعيب/التقدّم) + CI (GitHub Actions). (يبقى: e2e)
- [ ] توليد أنواع TypeScript من مخطط Supabase (`supabase gen types`).

## ملاحظات تقنية

- الاسم الداخلي `ghars` (سكيمة/رموز) ثابت؛ الاسم الظاهر «قِيَم».
- بيئة العرض الحالية: سكيمة `ghars` داخل مشروع Supabase مشترك (انظر `supabase/README.md`).

## مهم جدًا (طلب المالك) — لاحقًا
- [ ] **ربط أرقام صفحة /showcase ببيانات حقيقية** من قاعدة البيانات (عدد الأسر/الأطفال/المهام المعتمدة الفعلية) بدل البيانات التوضيحية.
- [ ] **رسم بياني للنمو** (أسر/نشاط عبر الزمن) من بيانات حقيقية.
