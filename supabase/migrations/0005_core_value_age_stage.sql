-- =============================================================================
-- منصة قِيَم — ربط القيمة بفئة عمرية
-- Migration 0005_core_value_age_stage
--
-- قرار #2: القيم الأربع نهائية الآن، مع إتاحة إضافة قيم لاحقًا من لوحة التحكم
-- وتحديد فئتها العمرية. هذا العمود يدعم ذلك الربط الاختياري.
-- =============================================================================

alter table core_values
  add column if not exists age_stage_id uuid references age_stages(id) on delete set null;
